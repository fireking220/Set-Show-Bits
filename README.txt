SetBits

This program takes in args from the user and turns on those bits in the binary representation of the number.

ShowBits

This program takes in args from the user represented as hex values and outputs what bits are both 1 in those 2 hex values, it will then output that new binary number in an unsigned and signed format